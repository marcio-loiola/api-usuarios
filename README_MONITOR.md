## LinkedIn Job Monitor (MVS)

- Python 3.13, Playwright (planned), SQLite, Telegram/Email notifications.
- Configure `.env` from `.env.example`.
- Run: `python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`.
- Future: add Playwright login and scraping to replace `fake_linkedin_scan`.
